services:
  - type: web
    name: lineup-api
    runtime: node
    region: oregon
    plan: starter
    rootDir: tv-guide-app/server
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
